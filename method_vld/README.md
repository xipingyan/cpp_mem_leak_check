# Check memory leak via VLD (Visual Leak Detect)

Here is a sample based on Visual Leak Detect.

#### How run sample

    $ cd [PATH]\cpp_mem_leak_check_windows\method_vld
    $ mkdir build
    $ cd build
    $ cmake-gui  // Generate project and build.

# Refer

    1: 
